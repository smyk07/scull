-include "io.scl"

fn main() : int {
  int number

  printf("Enter a number: ")
  scanf("%d", &number)
  
  int count = 0
  
  loop {
    if number == 0 break
    number = number / 10
    count = count + 1
    continue
  }
  
  printf("Number of digits: %d\n", count)
  
  return 0
}
